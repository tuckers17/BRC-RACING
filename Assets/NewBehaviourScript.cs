using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NewBehaviourScript : MonoBehaviour
{
    // Start is called before the first frame update
    bool reeceIsThin = false;
    void Start()
    {
       Debug.Log("hi jack"); 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
} 